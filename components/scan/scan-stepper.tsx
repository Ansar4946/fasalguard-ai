import type {ScanStep} from "@/features/diagnosis/types";
const steps:Array<{key:ScanStep;label:string}>=[{key:"field",label:"Select Field"},{key:"capture",label:"Upload Images"},{key:"symptoms",label:"Symptoms"},{key:"analysis",label:"AI Analysis"},{key:"results",label:"Review"}];
export function ScanStepper({current}:{current:ScanStep}){const active=steps.findIndex(step=>step.key===current);return <nav aria-label="Crop scan progress" className="overflow-x-auto border-b border-brand/10 py-4"><ol className="flex min-w-[560px] items-center">{steps.map((step,index)=><li key={step.key} className={`flex items-center ${index<steps.length-1?"flex-1":""}`} aria-current={index===active?"step":undefined}><span className="flex flex-col items-center gap-1.5"><span className={`grid size-7 place-items-center rounded-full text-[10px] font-extrabold ${index<=active?"bg-brand text-white":"bg-[#e4e8df] text-muted"}`}>{index<active?"✓":index+1}</span><span className={`whitespace-nowrap text-[9px] font-bold ${index===active?"text-brand":"text-muted"}`}>{step.label}</span></span>{index<steps.length-1&&<span className={`mx-2 h-0.5 flex-1 ${index<active?"bg-brand":"bg-border"}`}/>}</li>)}</ol></nav>}
