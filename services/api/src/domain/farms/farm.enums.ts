export enum FarmStatus {
  Active = 'active',
  Archived = 'archived',
}
export enum FieldStatus {
  Active = 'active',
  Fallow = 'fallow',
  Archived = 'archived',
}
export enum CropCycleStatus {
  Planned = 'planned',
  Active = 'active',
  Harvested = 'harvested',
  Cancelled = 'cancelled',
}
