"use client";export default function GovernmentError({reset}:{error:Error&{digest?:string};reset:()=>void}){return <div className="page-container"><section className="card mx-auto max-w-xl p-8 text-center" role="alert"><p className="text-sm font-bold text-danger">Analytics unavailable</p><h1 className="mt-2 text-2xl font-extrabold">We could not load governance data</h1><p className="mt-2 text-sm text-muted">No report or advisory was changed. Retry when the connection is stable.</p><button onClick={reset} className="mt-5 min-h-11 rounded-xl bg-brand px-5 font-bold text-white">Try again</button></section></div>}
