"use client";
export default function ExpertError({reset}:{error:Error&{digest?:string};reset:()=>void}){return <div className="page-container"><section className="card mx-auto max-w-xl p-8 text-center" role="alert"><p className="text-sm font-bold text-danger">Workspace unavailable</p><h1 className="mt-2 text-2xl font-extrabold">We could not load expert data</h1><p className="mt-2 text-sm text-muted">No review decision was changed. Check the connection and retry.</p><button onClick={reset} className="mt-5 min-h-11 rounded-xl bg-brand px-5 font-bold text-white">Try again</button></section></div>}
