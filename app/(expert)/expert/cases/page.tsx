import Link from "next/link";
import { expertCases } from "@/features/expert/data";
export default function CaseQueuePage(){return <div className="page-container space-y-5"><header><p className="text-sm font-bold text-success">Expert review</p><h1 className="mt-1 text-3xl font-extrabold">Case queue</h1><p className="mt-2 text-sm text-muted">Review AI-suspected cases ordered by risk and waiting time.</p></header><section className="grid gap-3">{expertCases.map(item=><Link key={item.id} href={`/expert/cases/${item.id}`} className="card flex flex-col gap-3 p-5 hover:border-brand sm:flex-row sm:items-center"><div className="flex-1"><p className="text-xs font-bold uppercase text-muted">{item.id} · {item.priority}</p><h2 className="mt-1 font-extrabold">{item.aiSuggestion}</h2><p className="mt-1 text-sm text-muted">{item.farmerName} · {item.crop} · {item.district}</p></div><div className="flex gap-2 text-xs font-bold"><span className="rounded-full bg-brand-soft px-3 py-2">{Math.round(item.confidence*100)}% confidence</span><span className="rounded-full bg-orange-100 px-3 py-2 text-orange-900">{item.severity}</span></div></Link>)}</section></div>}
