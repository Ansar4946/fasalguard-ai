import { notFound } from "next/navigation";
import Link from "next/link";
import { DiseaseGuide } from "@/components/learning/disease-guide";
import { learningArticles } from "@/features/learning/data";
export function generateStaticParams(){return learningArticles.map(x=>({slug:x.slug}))}
export default async function LearningArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const article=learningArticles.find(x=>x.slug===slug);if(!article)notFound();if(slug==="cotton-leaf-curl")return <DiseaseGuide/>;return <div className="page-container"><article className="card mx-auto max-w-3xl p-7"><Link href="/learning" className="text-sm font-bold text-brand">← Knowledge Centre</Link><p className="mt-6 text-xs font-bold uppercase text-success">{article.category} · {article.level}</p><h1 className="mt-2 text-3xl font-extrabold">{article.title}</h1><p className="mt-4 text-base leading-7 text-muted">{article.summary}</p><div className="mt-6 rounded-xl bg-amber-50 p-4 text-sm leading-6 text-amber-950"><strong>Content preparation:</strong> This supporting guide is awaiting detailed local expert review. The cotton leaf curl guide demonstrates the complete production article format.</div><Link href="/learning/cotton-leaf-curl" className="mt-5 inline-flex min-h-11 items-center rounded-xl bg-brand px-5 text-sm font-bold text-white">View complete example guide</Link></article></div>}
