"use client";
export default function ScanError({ reset }: { error: Error & { digest?: string }; reset: () => void }) { return <div className="page-container"><section className="card mx-auto max-w-xl p-7 text-center" role="alert"><p className="text-sm font-bold text-danger">Scan unavailable</p><h1 className="mt-2 text-2xl font-extrabold">We could not open this scan step</h1><p className="mt-2 text-sm text-muted">Your saved scan draft is safe. Check your connection and try again.</p><button type="button" onClick={reset} className="mt-5 min-h-11 rounded-xl bg-brand px-5 text-sm font-bold text-white">Try again</button></section></div>; }
